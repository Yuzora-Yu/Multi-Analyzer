# v4.3 共通判定・出来高研究

## 結論

利益上の優位性はまだ確認できていません。見た目の完成や条件の厳しさを、勝率の高さとは扱いません。
従来の多数指標スコアは候補が少なすぎ、リボンを追加するだけでも改善しませんでした。
説明・記録が可能な独立Pルールを**検証用の候補**として採用しました。利益を示した完成戦略としての採用ではありません。
S/A/Bは条件の分類、V/VRは出来高の強さであり、異なる時間足や異なる分類間の収益順位はありません。

## 共通判定

Bybit XAUUSDT無期限契約とBTCUSDT現物の15分・1時間・4時間足をCloudflareが取得。
1分ごとに確認し、15分足の確定から最低2秒経過した足を判定します。
通知とブラウザの15分足は、同一スナップショット・設定・エンジンを使います。
ライブ価格はWebSocketで別更新し、形成中の動きで共通判定を変更しません。
メールURLには判定IDを含み、直近24判定と、各銘柄の送信済み直近100通知の記録を再生できます。期限切れはエラー表示し、現在の推奨にすり替えません。
5分・1時間足とCSVは個別の研究画面で、クラウド通知対象ではありません。
共通計算の資金は1,000 USD、リスク0.5%。ユーザーの実口座残高ではありません。

## 参考資料と独自の定義

ユーザー提供の文章と画像は別時点の参考資料。FAD APEXのソース・未公開閾値は取得しておらず、同一製品の複製とは主張しません。

- リボン: EMA 5/8/13/21/34/55/89/144。
- 高安: 左右3本のピボットを、右3本が確定した時点で認識。高安が共に切り上がれば上、共に切り下がれば下。
- 転換: 高安方向とEMA5対144が一致、ADX>=20、リボン幅>=0.25ATR、直近転換から5本以上、2本連続の転換票。成立しなければ前方向を保持。保持方向と現在のEMA向きは別表示。
- P: 15分足以上、継続方向、直前4本以内にEMA21へ接触しEMA13の反対側で引ける、直前足はEMA13反対側、現在の確定足でEMA13を終値奪還、方向と足色が一致。
- リボン健全性: EMA21/55/144が継続方向に順序整列し、EMA5対144も同方向。8本すべての順序整列を要求すると押し目そのものを排除するため、長めの3本で定義。
- P追加条件: 既に確定したH1方向一致、出来高>=直前21本平均、ADX>=20、データ品質とコスト後R:R。基準値は買い/売り対称。
- V: 直前21本平均の1.3倍以上。VR: 1.8倍以上かつ実体>=1ATR。独自の初期値で、元製品の閾値ではありません。
- バッジなしB、バッジありA、H1一致かつバッジありS。SQの未公開条件は推測実装しません。
- 黄EXIT注意: EMA13の反対側で2本確定した時点。天底へ後付け表示せず、反転エントリーの指示とも扱いません。
- 実際の保有入力に対する撤退推奨はSL・反対確定候補と構造・H1反転などの既存ポジション判定を使用。黄EXIT注意と区別します。
- 吸収候補: 出来高>=1.8倍、値幅<=0.8ATR、実体<=0.25ATR。価格に対して出来高が多いというOHLCV上の仮説で、大口注文の直接観測ではありません。
- 価格帯別出来高: 直近96本、48価格帯。各足の出来高を高安範囲へ一様配分する近似。POCは最大帯、VAH/VALはPOCから隣接帯を加えて70%を覆う範囲。真の約定価格分布ではありません。
- SMC: 既存のBOS/CHoCH、流動性スイープ、FVG、OB、EQH/EQL、PDH/PDL、プレミアム/ディスカウント、確定ピボットのRSI乖離を併記。
- 真の成行売買差は提供されたtaker buy量がある場合だけ計算。Bybitローソク足には含まれないため、現在の通知でCVDや大口の正体を推定しません。

## 実データ比較（2026-09-08）

各銘柄6,000本を取得。評価対象は2026-07-10 23:30 UTC〜09-07 20:00 UTC。
2026-08-14 08:30 UTCで開発側60%と確認側40%に分割。
未来の足を除外し、直近299本の分析窓、次足始値で約定、同じ足のSL/TP衝突はSL優先。
片道手数料2bps、スプレッド1.8bps、スリッページ1.2bps。実際の費用を保証する値ではなく、資金調達率は未算入。
固定TP/SL・部分利確・時間退出のシミュレーションであり、画面の裁量EXITを含む実運用成績ではありません。

| 条件 | 金 全期間 | BTC 全期間 | 金 確認側 | BTC 確認側 |
|---|---:|---:|---:|---:|
| 従来スコア | 2件 / -2.20R | 0件 | 1件 / -1.14R | 0件 |
| 従来＋リボン一致 | 2件 / -2.20R | 0件 | 1件 / -1.14R | 0件 |
| 従来＋P必須 | 0件 | 0件 | 0件 | 0件 |
| 独立P | 9件 / -1.66R | 9件 / -1.63R | 3件 / -0.26R | 2件 / +2.01R |

独立Pの全期間勝率は金55.6%、BTC44.4%でも期待値は負です。勝率だけでは採用可否を決められません。
BTC確認側の2勝は統計的証拠として小さすぎます。比較を見た後に選んだPモデルを「未使用データで検証済み」とは呼びません。
次の評価は、これ以降の新しい相場での前向き記録が必要です。
詳細: [機械可読結果](validation-v43.json)。再実行: `node research-validation.js`（元データはGit非公開の.runtimeにキャッシュ）。

## 通知の範囲

新規P候補をメール送信。送信済み候補を仮の保有として追跡し、撤退条件または12時間経過で「前回候補を保有中なら」と案内します。
実際にユーザーが注文したとは仮定せず、ブローカー口座を読み書きしません。新規候補の初回起動時の一斉送信は抑止します。
通知の確認は15分確定足ベースなので、実際のストップ注文の代わりにはなりません。
無料枠の上限・提供元障害時は通知を停止し、更新停止を表示します。

## 主な一次資料

- [TradingView: Volume profile](https://www.tradingview.com/support/solutions/43000502040-volume-profile-indicators-basic-concepts/): POC・value areaとデータ種類による限界。
- [Bybit Kline API](https://bybit-exchange.github.io/docs/v5/market/kline): OHLCV、未確定足、出来高単位。
- [Bybit Kline WebSocket](https://bybit-exchange.github.io/docs/v5/websocket/public/kline): confirmフラグ。
- [Bybit Public Trades](https://bybit-exchange.github.io/docs/v5/market/recent-trade): taker側を含む実約定。現在のKlineに存在しない情報と区別。
- [Bybit TradFi Perpetuals](https://www.bybit.com/en/learn/bybit-tradfi/trade-tradfi-perpetuals-bybit): 金無期限契約の性質。
- [Cloudflare DO pricing](https://developers.cloudflare.com/durable-objects/platform/pricing/): 無料枠。
