# 現行 Multi-Analyzer 点検結果

対象: `Yuzora-Yu/Multi-Analyzer` の `main/index.html`

## 総評

現行版は、Gold・Silver・BTCの相場構造、流動性、Order Block、FVG、Premium/Discountを一画面にまとめた試作品としてはよくできています。一方、実際のGold取引判断に使用するには、検証性・コスト評価・時間帯処理・損失制御が不足しています。

最も大きい問題は、**表示用分析と取引ロジックが単一HTMLに密結合し、売買結果を検証するバックテストがないこと** です。

## 主要な問題

### 1. 売買判定が単一方向・固定点数

`calcScore()` は1H/4Hのバイアスから一方向を決め、HTF、構造、P/D、流動性、OB/FVGを固定配点で合算しています。LONGとSHORTを独立比較せず、45点からエントリー候補を生成します。

影響:

- 買いと売りの優位差が小さくても一方向に寄る
- 強いトレンド、レンジ、急変動を同じ配点で扱う
- 45点という低い閾値でも注文候補が出る
- 過熱、伸び切り、データ品質、イベントを拒否できない

強化版:

- LONG/SHORTを独立採点
- 方向差8点未満を見送り
- WATCH 58、READY 70、STRONG 82
- スコアより先にhard vetoを評価

### 2. コスト後R:Rを評価していない

`genEntry()` はOrder Blockがなければ現在値をEntryとし、SLを1.5ATR、TPを2ATR/4ATRで生成します。手数料、スプレッド、スリッページ、資金調達費を考慮していません。

影響:

- 5分足では取引コストがストップ幅に対して過大になりやすい
- 表面上2Rでも、コスト後は1R未満になり得る
- 指値と成行の違いを扱わない

強化版:

- コスト後R:Rが1.8未満なら拒否
- CLOSE確認とLIMIT RETESTを区別
- 口座資金・許容損失・最大レバレッジから数量計算

### 3. バックテストが存在しない

現行コードは現在チャートを再計算して表示しますが、過去の各時点で当時利用可能だった情報だけを使う検証機能がありません。

影響:

- 勝率、期待値、最大ドローダウン、連敗、方向別成績が不明
- ルール変更が改善なのか悪化なのか判断できない
- Order Block/FVGの過去状態を検証できない

強化版:

- 指標を前計算し、各時点で確定済み足のみ参照
- 次足約定、同一足ストップ優先
- コスト込みR損益
- IS/OOS分離
- クールダウン、日次損失停止

### 4. セッション時刻が固定JSTで、DSTと履歴時刻に弱い

現行 `calcSessions()` は `Date.now()` を基準に当日の範囲を作り、London/NYを固定JST時刻で扱います。また `getKZ()` のLondon Openは `1560–1620` 分ですが、JST分は0–1439のため一致しません。

影響:

- 夏時間・冬時間でLondon/NY判定がずれる
- 過去データを表示しても「現在日」のセッションを参照する
- London OpenのKZが実質到達不能

強化版:

- `Europe/London`、`America/New_York`、`Asia/Tokyo` のIANA timezoneを使用
- 各ローソク足の時刻でDSTを自動反映
- NYロールオーバー周辺をhard veto

### 5. データ品質ゲートがない

現行はAPIが配列を返せば利用し、欠損、重複、時間ギャップ、古い最終足を売買判定から除外しません。

強化版:

- OHLC整合性、重複、並び順を正規化
- 履歴本数、時間足ギャップ、最終確定足の鮮度を検査
- 問題があれば売買候補を出さない

### 6. リスク制御がない

現行はEntry/SL/TPを表示しますが、次がありません。

- 1回の許容損失
- 最大レバレッジ
- 損切り後の再エントリー待機
- 連敗時停止
- 日次損失上限
- 分割決済と建値移動

強化版では、0.5%リスクを標準とし、TP1/TP2/TP3、建値移動、クールダウン、日次停止を実装しました。

### 7. 単一HTMLへの密結合

現行 `index.html` は約1,000行で、CSS、HTML、データ取得、WebSocket、分析、描画、取引候補生成が同居しています。

影響:

- ロジック単体テストが困難
- UI変更が分析ロジックへ影響しやすい
- バージョン比較とレビューが難しい

強化版:

- `strategy-core.js`: 純粋ロジック
- `app.js`: UI・通信
- `styles.css`: 表示
- `server.py`: 同一オリジンAPI
- `tests`: ロジック試験

## 維持した考え方

現行版のうち、次の考え方は有用なため、より厳密な形で残しています。

- BOS / CHoCH
- Swing High / Swing Low
- Liquidity sweep
- FVG
- Premium / Discountに相当する価格位置
- 1H / 4Hの上位足整合
- ATRベースのストップ
- セッション別の流動性差

## テスト済み事項

`npm test` で次を確認しています。

- OHLC正規化と重複除去
- スイングが右側足確定前に漏れないこと
- 未完成の上位足を集計しないこと
- LONG/SHORT分析と数量計算が成立すること
- CSV解析
- バックテストが例外なく完了すること

## 残存リスク

この強化版でも、次は保証できません。

- 将来の利益
- 急変時の実約定価格
- ブローカー停止、API停止、取引所障害
- 経済指標直後のギャップとスリッページ
- XAUUSDTと実際に取引するXAUUSD/COMEXの価格差
- 選択したパラメータの将来安定性

まず最低4–8週間のペーパートレードを行い、シグナル時刻、提示価格、実際に約定可能だった価格、最大逆行、最大順行を記録して再評価してください。
