{
  "name": "multi-analyzer-ultimate",
  "version": "4.1.0",
  "private": true,
  "description": "Rule-based multi-timeframe market analysis and paper-trading dashboard",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "node --check app.js && node --check strategy-core.js && node --check smc-core.js && node --check monitor.js && node --check backtest-worker.js"
  },
  "engines": {
    "node": ">=20"
  }
}
